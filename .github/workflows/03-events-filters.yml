name: 03-events-filters

on:
  # pull_request:
  #   types: [closed]
  #   types:
  #     - opened
  #     - closed
  push:
    branches:
      - main
      - master

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello world"
