name: 03-events-filters

on:
  # pull_request:
  #   types: [closed]
  #   types:
  #     - opened
  #     - closed
  push:
    # branches:
    #   - main
    #   - master
    # branches-ignore:
    #   - main
    #   - master
    # tags:
    #   - 'v*'
    # tags-ignore:
    #   - 'v*'
    # paths:
    #   - "routes/**" // only trigger if files in the routes directory are changed
        # - "routes/*" // only trigger if files in the routes directory are changed, but not in subdirectories
    paths-ignore:
      - "routes/**"

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello world"
